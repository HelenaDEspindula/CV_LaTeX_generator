---
title: "Curriculum Vitae - Helena R. S. D'Espondula"
author: "helena.despindula@gmail.com"
date: "`r format(Sys.time(), '%d/%m/%Y')`"
output: pdf_document
papersize: a4
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
options(knitr.table.format = "latex")
```

## Dados pessoais

```{r xml}

file_name <- "CV-2021-dados_pessoais.csv"

tabela_imprimir <- read.csv2(file_name, sep = ";", header = FALSE)

#View(tabela_imprimir)

knitr::kable(tabela_imprimir, align = "ll", "latex", col.names = NULL)

```


```{r links}

file_name <- "CV-2021-links.csv"

tabela_imprimir <- read.csv2(file_name, sep = ";", header = FALSE)

#View(tabela_imprimir)

knitr::kable(tabela_imprimir, align = "ll", "latex", col.names = NULL)

```

## Formacao

```{r csv}

file_name <- "CV-2021-Formacao.csv"

tabela_imprimir <- read.csv2(file_name, sep = ";", header = FALSE)

#View(tabela_imprimir)

knitr::kable(tabela_imprimir, align = "llll", "latex", col.names = NULL)
```


